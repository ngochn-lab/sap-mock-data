spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3307}/${MYSQL_DATABASE:spring_cloud_function_demo}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:admin123}
  jpa:
    hibernate:
      ddl-auto: update    # dev: update; production: validate or none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8088

# Application Insights
azure:
  application-insights:
    connection-string: ${APPLICATION_INSIGHTS_CONNECTION_STRING:}
